version: '3.1'

services:
  db:
    image: postgres
    restart: always
    ports: 
      - "5432:5432"
    environment:
      POSTGRES_PASSWORD: dummy
  dbv2:
    image: postgres
    restart: always
    ports: 
      - "5555:5432"
    environment:
      POSTGRES_PASSWORD: ahl@123
